-r requirements.txt
pytest>=7.0
pytest-asyncio>=0.21
pytest-mock>=3.10
# Lint + type-check (run in CI). Lenient to start — see ruff.toml and
# the CI workflow; type-check is non-blocking until the legacy modules
# are annotated.
ruff>=0.6
mypy>=1.8
